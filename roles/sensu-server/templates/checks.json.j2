## DELETE ME

{
  "_comment": "{{ ansible_managed }}",

  "checks": {

    "check_load": {
      "command": "/etc/sensu/plugins/check-load.rb",
      "interval": 30,
      "subscribers": [ "production" ]
    },

    "check_cpu": {
      "command": "/etc/sensu/plugins/check-cpu.rb -w 60 -c 80",
      "interval": 30,
      "subscribers": [ "production" ]
    },

    "cpu_metrics": {
      "type": "metric",
      "handlers": [ "graphite" ],
      "command": "/etc/sensu/plugins/cpu-metrics.rb",
      "subscribers": [ "production" ],
      "interval": 30,
      "handler": "debug"
    }


  }

}
